A wrapper around the `<Checkbox>` component pre-configured with `useMuiCheckboxAdapter`.

## <span className="docs-h2">Import</span>

```tsx
import { MuiCheckboxElement } from '@piplup/rhf-adapters/mui-material';
```
