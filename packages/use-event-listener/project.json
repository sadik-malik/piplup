{
  "name": "@piplup/use-event-listener",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/use-event-listener/src",
  "projectType": "library",
  "tags": ["scope:packages"],
  "targets": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": [
        "default",
        "^default",
        {
          "externalDependencies": ["rollup"]
        }
      ]
    }
  }
}
